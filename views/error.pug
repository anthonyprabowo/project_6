extends layout.pug 

block content 
    h1 Opps! it looks like something went wrong on ther server. :(
    p= err.status 
    p= err.stack

    nav.nav
      a(href='/')
        span &larr;
        |  Back